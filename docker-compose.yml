version: '3.8'
services:
  nudge:
    image: ghcr.io/kommandantredundant/nudge:latest
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped