frontend
    functionality
        - in the circle management in options, i cannot change the days, it is shown as a form field, but its not editable. it should be editable and when the value is changed, a checkbox should appear next to it, where i can click/tap to accept and save the new value
        -  when accessing the app via browser, the should be a pwa prompt to install it as an app. When an app is installed and the app is accessed via browser, it should switch to the installed app.
    design
        - Header has no background, so scrolled content is visible behind it. It should have the default background color and a shadow at the bottom to have a visible line between header and content background
        - the options/settings wheel should be on the right, not below the titel 
        - the add contact button should be in the header bar as well, left of the settings wheel